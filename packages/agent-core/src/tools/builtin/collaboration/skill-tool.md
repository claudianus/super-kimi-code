Invoke one registered skill by exact name. Use `SearchSkill` first when you need discovery, then call this tool with the exact candidate name. Direct user slash activation may also load a skill. Do not re-invoke a skill to repeat work already done: if a `<kimi-skill-loaded>` block for it with the same `args` is already present in the conversation, follow those instructions directly instead of calling the tool again. Do call the tool again when you need the skill with different arguments — the loaded block was expanded with the earlier `args` and will not reflect new inputs.
