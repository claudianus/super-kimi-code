---
"@moonshot-ai/kimi-code": minor
---

Add provider credential pools with quota-aware fallback routing, route health diagnostics, and CLI/TUI management. Use `kimi provider key add`, `kimi provider oauth add`, `kimi provider route auto`, and `kimi provider route status` to manage routes without exposing secrets.
